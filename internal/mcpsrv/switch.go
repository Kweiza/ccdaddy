package mcpsrv

import (
	"github.com/modelcontextprotocol/go-sdk/mcp"

	"github.com/Kweiza/ccdaddy/internal/view"
)

// addSwitchTool registers switch, the one tool that rewrites the live Claude
// Code credentials, together with the channel that asks the person at the
// keyboard before it does. It already has a class in this package's class map;
// this is where its schema, its handler and that confirm go, and until they
// arrive the map is what refuses it.
func addSwitchTool(srv *mcp.Server, e view.Exec) error { return nil }
